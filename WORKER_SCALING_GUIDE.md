# Руководство по масштабированию воркеров очереди

## Как определить сколько воркеров нужно?

### 🚀 Автоматический способ (рекомендуется)

#### 1. Команда для анализа и автоматической настройки:

```bash
# Показать рекомендации без изменений
php artisan queue:optimize-workers --dry-run

# Автоматически настроить оптимальное количество
php artisan queue:optimize-workers --auto

# Интерактивный режим (с подтверждением)
php artisan queue:optimize-workers
```

Пример вывода:
```
🔍 Анализ нагрузки очереди...

+----------------------+----------+
| Метрика              | Значение |
+----------------------+----------+
| Активных аккаунтов   | 3        |
| Джоб в очереди       | 15       |
| Готовых к выполнению | 8        |
| Отложенных           | 7        |
| Запущено воркеров    | 2        |
| Упавших джоб (24ч)   | 5        |
+----------------------+----------+

🎯 Рекомендуемое количество воркеров:
   Минимум: 3
   Оптимально: 4
   Максимум: 5

⚠️ Недостаточно воркеров! У вас: 2, нужно минимум: 3

Запустить 4 воркеров? (yes/no) [yes]:
```

#### 2. Подробный анализ нагрузки:

```bash
php analyze-queue-load.php
```

Показывает:
- Количество активных аккаунтов
- Текущую очередь и типы джоб
- Запущенные воркеры
- Упавшие джобы за 24 часа
- Рекомендации по количеству воркеров
- Прогноз нагрузки по времени суток

---

## 📊 Формула расчёта

### Базовая формула:

```
Оптимальное количество = Активные аккаунты × Коэффициенты

Где коэффициенты зависят от:
1. Размера очереди (Queue Factor)
2. Количества ошибок (Fail Factor)
3. Времени суток (Time Factor)
```

### Детальный расчёт:

#### 1. **Базовое количество**
```
Base = max(2, Активные аккаунты)

Минимум 2 воркера даже для 1 аккаунта
```

#### 2. **Коэффициент очереди** (Queue Factor)
```
Если готовых джоб > 50:  Queue Factor = 2.0
Если готовых джоб > 20:  Queue Factor = 1.5
Иначе:                   Queue Factor = 1.0
```

#### 3. **Коэффициент ошибок** (Fail Factor)
```
Если упавших > 20 за 24ч:  Fail Factor = 1.5
Если упавших > 5 за 24ч:   Fail Factor = 1.2
Иначе:                     Fail Factor = 1.0
```

#### 4. **Коэффициент времени** (Time Factor)
```
Пиковые часы (9:00-18:00):  Time Factor = 1.0
Ночные часы:                Time Factor = 0.8
```

#### 5. **Итоговая формула:**
```
Минимум   = Base × 0.8 × Time Factor
Максимум  = Base × Queue Factor × Fail Factor
Оптимум   = (Минимум + Максимум) / 2
```

---

## 🎯 Практические примеры

### Пример 1: Стабильная работа
```
Аккаунтов: 3
Джоб в очереди: 5
Упавших за 24ч: 0
Время: 14:00 (пик)

Расчёт:
Base = 3
Queue Factor = 1.0 (мало джоб)
Fail Factor = 1.0 (нет ошибок)
Time Factor = 1.0 (пик)

Минимум = 3 × 0.8 × 1.0 = 2.4 → 3
Максимум = 3 × 1.0 × 1.0 = 3
Оптимум = (3 + 3) / 2 = 3

✅ Рекомендация: 3 воркера
```

### Пример 2: Большая нагрузка
```
Аккаунтов: 5
Джоб в очереди: 60
Упавших за 24ч: 3
Время: 15:00 (пик)

Расчёт:
Base = 5
Queue Factor = 2.0 (>50 джоб)
Fail Factor = 1.0 (мало ошибок)
Time Factor = 1.0 (пик)

Минимум = 5 × 0.8 × 1.0 = 4
Максимум = 5 × 2.0 × 1.0 = 10
Оптимум = (4 + 10) / 2 = 7

✅ Рекомендация: 7 воркеров
```

### Пример 3: Много ошибок
```
Аккаунтов: 4
Джоб в очереди: 25
Упавших за 24ч: 15
Время: 11:00 (пик)

Расчёт:
Base = 4
Queue Factor = 1.5 (>20 джоб)
Fail Factor = 1.2 (>5 ошибок)
Time Factor = 1.0 (пик)

Минимум = 4 × 0.8 × 1.0 = 3.2 → 4
Максимум = 4 × 1.5 × 1.2 = 7.2 → 8
Оптимум = (4 + 8) / 2 = 6

✅ Рекомендация: 6 воркеров
⚠️ + Проверить причину ошибок!
```

### Пример 4: Ночное время
```
Аккаунтов: 3
Джоб в очереди: 8
Упавших за 24ч: 1
Время: 02:00 (ночь)

Расчёт:
Base = 3
Queue Factor = 1.0
Fail Factor = 1.0
Time Factor = 0.8 (ночь)

Минимум = 3 × 0.8 × 0.8 = 1.92 → 2
Максимум = 3 × 1.0 × 1.0 = 3
Оптимум = (2 + 3) / 2 = 2.5 → 3

✅ Рекомендация: 2-3 воркера (можно сократить до 2)
```

---

## 📈 Динамическое масштабирование

### Автоматическое масштабирование по расписанию

Добавьте в cron:

```cron
# Каждый час анализировать и оптимизировать
0 * * * * cd /path/to/sellermind-ai && php artisan queue:optimize-workers --auto >> storage/logs/worker-optimization.log 2>&1

# Пиковые часы - больше воркеров (9:00)
0 9 * * * cd /path/to/sellermind-ai && ./start-queue-workers.sh 5

# Ночь - меньше воркеров (22:00)
0 22 * * * cd /path/to/sellermind-ai && ./start-queue-workers.sh 2
```

---

## 🔍 Мониторинг и индикаторы

### Когда нужно УВЕЛИЧИТЬ количество воркеров:

1. **Очередь растёт**
   ```bash
   php artisan queue:monitor database
   # Если постоянно > 20 джоб
   ```

2. **Много упавших джоб**
   ```bash
   php artisan queue:failed | wc -l
   # Если > 10 за час
   ```

3. **Медленная обработка**
   ```bash
   # Джобы ждут в очереди > 5 минут
   ```

### Когда можно УМЕНЬШИТЬ количество воркеров:

1. **Очередь всегда пустая**
   ```bash
   # Если постоянно 0-3 джобы
   ```

2. **Низкая загрузка CPU**
   ```bash
   top
   # Воркеры используют < 10% CPU
   ```

3. **Ночное время**
   ```bash
   # 22:00 - 08:00
   ```

---

## ⚙️ Команды для управления

### Анализ и настройка

```bash
# Автоматический анализ и оптимизация
php artisan queue:optimize-workers --auto

# Только показать рекомендации
php artisan queue:optimize-workers --dry-run

# Подробный анализ
php analyze-queue-load.php

# Проверка статуса
php test-queue-status.php
```

### Ручное управление

```bash
# Запустить 5 воркеров
./start-queue-workers.sh 5

# Проверить сколько запущено
ps aux | grep queue:work | grep -v grep | wc -l

# Остановить все
pkill -f "queue:work"

# Перезапустить
php artisan queue:restart
./start-queue-workers.sh 3
```

---

## 📊 Таблица рекомендаций

| Аккаунтов | Джоб в очереди | Упавших/24ч | Время  | Мин | Оптимум | Макс |
|-----------|----------------|-------------|--------|-----|---------|------|
| 1-2       | 0-10           | 0-2         | Пик    | 2   | 2       | 2    |
| 1-2       | 0-10           | 0-2         | Ночь   | 2   | 2       | 2    |
| 3-5       | 0-10           | 0-2         | Пик    | 3   | 3-4     | 5    |
| 3-5       | 0-10           | 0-2         | Ночь   | 2   | 3       | 4    |
| 3-5       | 20-50          | 0-5         | Пик    | 4   | 6       | 8    |
| 3-5       | >50            | >5          | Пик    | 5   | 8       | 10   |
| 6-10      | 0-10           | 0-2         | Пик    | 5   | 6-7     | 8    |
| 6-10      | 20-50          | 0-5         | Пик    | 6   | 9       | 12   |
| >10       | Любое          | Любое       | Пик    | 10  | 15      | 20   |

---

## 💡 Лучшие практики

### 1. **Регулярный мониторинг**
```bash
# Добавить в cron - проверять каждые 5 минут
*/5 * * * * cd /path/to/sellermind-ai && php artisan queue:monitor database
```

### 2. **Автоматическая оптимизация**
```bash
# Каждый час анализировать и корректировать
0 * * * * cd /path/to/sellermind-ai && php artisan queue:optimize-workers --auto
```

### 3. **Логирование**
```bash
# Сохранять результаты анализа
php analyze-queue-load.php >> storage/logs/queue-analysis.log
```

### 4. **Алерты при проблемах**
```bash
# Скрипт для проверки и уведомлений
#!/bin/bash
WORKERS=$(ps aux | grep queue:work | grep -v grep | wc -l)
if [ $WORKERS -eq 0 ]; then
    echo "ALERT: Queue workers stopped!" | mail -s "Queue Alert" admin@example.com
    ./start-queue-workers.sh 3
fi
```

### 5. **Использование Supervisor** (для продакшена)
```ini
[program:sellermind-queue]
numprocs=%(ENV_QUEUE_WORKERS)s  # Из переменной окружения
process_name=%(program_name)s_%(process_num)02d
command=php artisan queue:work --timeout=600
autostart=true
autorestart=true
```

---

## 🎯 Быстрая шпаргалка

```bash
# 1. Узнать сколько нужно воркеров
php artisan queue:optimize-workers --dry-run

# 2. Запустить рекомендуемое количество
php artisan queue:optimize-workers --auto

# 3. Проверить что работает
ps aux | grep queue:work | grep -v grep

# 4. Мониторить очередь
php artisan queue:monitor database

# 5. При проблемах - подробный анализ
php analyze-queue-load.php
```

---

## ❓ FAQ

**Q: Сколько воркеров нужно для X аккаунтов?**
A: Используйте команду `php artisan queue:optimize-workers --dry-run` - она посчитает автоматически на основе текущей нагрузки.

**Q: Можно ли запустить слишком много воркеров?**
A: Да, лишние воркеры будут простаивать и занимать память. Оптимум = количество аккаунтов + 20%.

**Q: Что делать если джобы накапливаются?**
A: 1) Проверьте что воркеры запущены, 2) Увеличьте их количество, 3) Проверьте нет ли медленных джоб.

**Q: Как часто менять количество воркеров?**
A: Используйте автоматическую оптимизацию раз в час. Для стабильной нагрузки достаточно настроить 1 раз.

**Q: Нужно ли перезапускать воркеры после изменения кода?**
A: Да! Выполните `php artisan queue:restart` или `./start-queue-workers.sh N`.

---

**Последнее обновление**: 2025-12-15
