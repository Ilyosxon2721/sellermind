# /implement - Реализация новой функции

Полный цикл разработки функции: анализ → план → код → тесты → review → коммит.

## Использование

```
/implement <описание функции>
```

## Примеры

```
/implement Добавить фильтрацию товаров по статусу
/implement Экспорт заказов в PDF
/implement Уведомление о низком остатке в Telegram
```

---

## Workflow

### Фаза 1: Анализ 🔍
```
□ Понять требования
□ Найти связанные файлы в проекте
□ Определить затрагиваемые модули
□ Проверить нет ли похожего функционала
```

**Вывод:** Краткий план что нужно сделать

### Фаза 2: Проектирование 📐
```
□ Использовать architect субагента
□ Определить:
  - Какие файлы создать/изменить
  - Структура БД (если нужна миграция)
  - API endpoints (если нужны)
  - UI компоненты (если нужны)
□ Создать план реализации
```

**Вывод:** Детальный план с файлами

### Фаза 3: Реализация 💻
```
□ Использовать backend субагента для:
  - Миграции
  - Модели
  - Сервисы
  - Контроллеры
  - Form Requests

□ Использовать frontend субагента для:
  - Blade компоненты
  - Alpine.js логика
  - Tailwind стили
```

**Вывод:** Рабочий код

### Фаза 4: Тестирование 🧪
```
□ Использовать tester субагента
□ Написать тесты:
  - Unit тесты для сервисов
  - Feature тесты для API/контроллеров
  - Browser тесты для UI (если критично)
□ Запустить все тесты
□ Исправить если падают
```

**Вывод:** Зелёные тесты

### Фаза 5: Review 👀
```
□ Использовать reviewer субагента
□ Проверить:
  - Качество кода
  - Безопасность
  - Производительность
  - Соответствие стандартам
□ Исправить замечания
```

**Вывод:** Код готов к коммиту

### Фаза 6: Завершение ✅
```
□ git add -A
□ git commit -m "feat(<scope>): <description>"
□ Обновить TASKS.md (если была задача)
□ Показать summary
```

---

## Правила

1. **Не пропускай фазы** — каждая важна
2. **Тесты обязательны** — не коммить без тестов
3. **Маленькие коммиты** — лучше несколько маленьких чем один большой
4. **Спрашивай если непонятно** — лучше уточнить чем сделать неправильно

---

## Вывод

После выполнения покажи:

```
✅ Реализовано: <название функции>

📁 Изменённые файлы:
  - app/Services/NewService.php (создан)
  - app/Http/Controllers/Controller.php (изменён)
  - resources/views/page.blade.php (создан)

🧪 Тесты: 5 passed, 12 assertions

📝 Коммит: feat(products): добавить фильтрацию по статусу (abc1234)
```
