# 🤖 SellerMind Autopilot для Windows

Полная автоматизация разработки с Claude Code CLI.

---

## 📦 Файлы

```
scripts/
├── autopilot.ps1        # Интерактивный режим
├── autopilot-silent.ps1 # Тихий режим (для планировщика)
└── run-autopilot.bat    # Быстрый запуск (двойной клик)
```

---

## 🚀 Установка

### 1. Скопируй файлы в проект

```powershell
# Создай папку scripts если нет
mkdir D:\server\OSPanel\home\sellermind\scripts

# Скопируй файлы туда
```

### 2. Проверь что API ключ установлен

```powershell
echo $env:ANTHROPIC_API_KEY
```

Если пусто — установи постоянно:
```powershell
[System.Environment]::SetEnvironmentVariable("ANTHROPIC_API_KEY", "sk-ant-твой-ключ", "User")
# Перезапусти PowerShell!
```

---

## 🎮 Использование

### Способ 1: Двойной клик
Просто запусти `run-autopilot.bat`

### Способ 2: PowerShell

```powershell
cd D:\server\OSPanel\home\sellermind

# Выполнить 3 задачи (по умолчанию)
.\scripts\autopilot.ps1

# Выполнить 5 задач
.\scripts\autopilot.ps1 -MaxTasks 5

# Выполнить конкретную задачу
.\scripts\autopilot.ps1 -TaskNumber "#003"

# Тестовый запуск (без выполнения)
.\scripts\autopilot.ps1 -DryRun
```

---

## ⏰ Автозапуск по расписанию (Task Scheduler)

### Шаг 1: Открой Планировщик заданий

```
Win + R → taskschd.msc → Enter
```

### Шаг 2: Создай задачу

1. **Действие** → **Создать задачу...**

2. **Вкладка "Общие":**
   - Имя: `SellerMind Autopilot`
   - Описание: `Автоматическая разработка с Claude Code`
   - ✅ Выполнять вне зависимости от регистрации пользователя
   - ✅ Выполнить с наивысшими правами

3. **Вкладка "Триггеры"** → **Создать:**
   - Начать задачу: По расписанию
   - Ежедневно
   - Время: `02:00:00` (или когда удобно)
   - ✅ Включено

4. **Вкладка "Действия"** → **Создать:**
   - Действие: Запуск программы
   - Программа: `powershell.exe`
   - Аргументы: 
     ```
     -ExecutionPolicy Bypass -File "D:\server\OSPanel\home\sellermind\scripts\autopilot-silent.ps1" -MaxTasks 3
     ```
   - Рабочая папка: `D:\server\OSPanel\home\sellermind`

5. **Вкладка "Условия":**
   - ❌ Запускать только при питании от сети (убрать если ноутбук)

6. **Вкладка "Параметры":**
   - ✅ Немедленно запускать задачу, если пропущен плановый запуск

7. **OK** → Введи пароль Windows

### Шаг 3: Проверь

```
ПКМ на задаче → Выполнить
```

Проверь лог:
```powershell
Get-Content D:\server\OSPanel\home\sellermind\.claude\autopilot.log -Tail 50
```

---

## 📋 После автопилота

Claude делает коммиты, но **НЕ пушит**. Утром проверь:

```powershell
cd D:\server\OSPanel\home\sellermind

# Посмотри коммиты
git log --oneline -10

# Проверь что изменилось
git diff HEAD~5 --stat

# Запусти тесты
php artisan test

# Если всё ок — пушь
git push origin develop
```

---

## 📊 Логи

```powershell
# Лог автопилота
Get-Content .\.claude\autopilot.log -Tail 100

# Лог ошибок
Get-Content .\.claude\autopilot-error.log -Tail 50

# Лог задач
Get-Content .\AUTOPILOT_LOG.md
```

---

## ⚙️ Настройка

### Изменить путь к проекту

В файлах `autopilot.ps1` и `autopilot-silent.ps1` измени:
```powershell
$ProjectPath = "D:\server\OSPanel\home\sellermind"
```

### Изменить количество задач

```powershell
.\scripts\autopilot.ps1 -MaxTasks 5
```

Или в Task Scheduler измени аргументы.

---

## 🛡️ Безопасность

### Что заблокировано:
- ❌ `git push` — запрещено полностью
- ❌ `rm -rf` — удаление файлов  
- ❌ `migrate:fresh` — очистка БД

### Git push только вручную:
```powershell
# Только после твоей проверки!
git push origin develop
```

---

## ❓ Проблемы

### "ANTHROPIC_API_KEY не установлен"
```powershell
[System.Environment]::SetEnvironmentVariable("ANTHROPIC_API_KEY", "sk-ant-...", "User")
# Перезапусти PowerShell и Task Scheduler!
```

### "Выполнение сценариев отключено"
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Task Scheduler не запускается
- Проверь что пароль Windows правильный
- Запусти вручную: ПКМ → Выполнить
- Проверь лог: `.claude\autopilot-error.log`

---

## 💰 Стоимость

API Anthropic платный:
- ~$0.01-0.10 за задачу
- ~$3-10 в месяц при активном использовании

Следи за использованием: https://console.anthropic.com/usage

---

Сделано с ❤️ для автоматизации SellerMind
