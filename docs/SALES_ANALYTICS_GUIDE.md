# Sales Analytics Dashboard Guide

**Version:** 1.0
**Date:** 2026-01-11

---

## Overview

Sales Analytics Dashboard предоставляет мощную аналитику продаж для принятия data-driven решений. Визуализация данных с графиками, топ/флоп товары, тренды и прогнозы.

**Ключевые возможности:**
- 📊 Реал-тайм дашборд с ключевыми метриками
- 📈 Графики продаж по дням
- 🏆 ТОП-10 самых продаваемых товаров
- 📉 Анализ худших товаров (флоп)
- 🎯 Продажи по категориям
- 🛒 Разбивка по маркетплейсам
- 📅 Гибкие периоды (сегодня, 7/30/90 дней)
- 💹 Сравнение с предыдущим периодом

---

## Quick Start

### 1. Открыть дашборд

Перейдите на `/analytics` для просмотра полного дашборда.

### 2. Выбрать период

В правом верхнем углу выберите период анализа:
- **Сегодня** — данные за текущий день
- **7 дней** — последняя неделя
- **30 дней** — последний месяц (по умолчанию)
- **90 дней** — последний квартал

### 3. Анализировать данные

Дашборд автоматически загрузит:
- Общая выручка с % роста
- Проданные единицы
- Количество заказов
- Средний чек
- График продаж по дням
- Топ-10 товаров
- Разбивка по категориям
- Статистика по маркетплейсам

---

## Ключевые метрики

### 1. Общая выручка (Total Revenue)

**Что показывает:** Общая сумма выручки за период

**Формула:**
```sql
SUM(price * quantity) за период
```

**Growth %:** Сравнение с предыдущим периодом
- 🟢 Положительный рост (зеленый)
- 🔴 Отрицательный рост (красный)

**Пример:** ₽ 1,234,567 ▲ 15.3% vs пред. период

---

### 2. Продано единиц (Total Sales)

**Что показывает:** Количество проданных товаров

**Формула:**
```sql
SUM(quantity) за период
```

**Использование:** Оценка объема продаж

---

### 3. Заказов (Total Orders)

**Что показывает:** Количество заказов за период

**Формула:**
```sql
COUNT(DISTINCT order_id) за период
```

**Использование:** Оценка активности клиентов

---

### 4. Средний чек (Average Order Value)

**Что показывает:** Средняя сумма заказа

**Формула:**
```
AOV = Total Revenue / Total Orders
```

**Использование:**
- Оптимизация ценообразования
- Выявление трендов покупательского поведения
- A/B тестирование акций

---

## API Endpoints

### Get Dashboard Data

Получить все данные дашборда одним запросом:

```http
GET /api/analytics/dashboard?period=30days
```

**Response:**
```json
{
  "overview": {
    "total_sales": 1234,
    "total_revenue": 567890.50,
    "total_orders": 456,
    "average_order_value": 1245.37,
    "revenue_growth_percentage": 15.3,
    "period": "30days",
    "date_from": "2025-12-12",
    "date_to": "2026-01-11"
  },
  "sales_by_day": {
    "labels": ["2025-12-12", "2025-12-13", ...],
    "quantities": [45, 67, 89, ...],
    "revenues": [12345.67, 23456.78, ...]
  },
  "top_products": [...],
  "sales_by_category": [...],
  "sales_by_marketplace": [...]
}
```

---

### Get Overview Only

```http
GET /api/analytics/overview?period=30days
```

**Response:**
```json
{
  "total_sales": 1234,
  "total_revenue": 567890.50,
  "total_orders": 456,
  "average_order_value": 1245.37,
  "revenue_growth_percentage": 15.3,
  "period": "30days",
  "date_from": "2025-12-12",
  "date_to": "2026-01-11"
}
```

---

### Get Sales by Day

Данные для графика продаж:

```http
GET /api/analytics/sales-by-day?period=30days
```

**Response:**
```json
{
  "labels": ["2025-12-12", "2025-12-13", "2025-12-14"],
  "quantities": [45, 67, 89],
  "revenues": [12345.67, 23456.78, 34567.89]
}
```

---

### Get Top Products

ТОП-10 самых продаваемых товаров:

```http
GET /api/analytics/top-products?period=30days&limit=10
```

**Response:**
```json
[
  {
    "product_id": 123,
    "product_name": "T-shirt Basic",
    "sku": "TSH-001",
    "total_quantity": 450,
    "total_revenue": 45000.00,
    "order_count": 234,
    "avg_price": 100.00
  },
  ...
]
```

---

### Get Flop Products

Худшие товары по продажам:

```http
GET /api/analytics/flop-products?period=30days&limit=10
```

**Response:** Аналогичен top-products, но отсортирован по возрастанию выручки

---

### Get Sales by Category

Разбивка продаж по категориям:

```http
GET /api/analytics/sales-by-category?period=30days
```

**Response:**
```json
[
  {
    "category_name": "Одежда",
    "total_quantity": 567,
    "total_revenue": 123456.78
  },
  {
    "category_name": "Обувь",
    "total_quantity": 234,
    "total_revenue": 89012.34
  }
]
```

---

### Get Sales by Marketplace

Разбивка по маркетплейсам:

```http
GET /api/analytics/sales-by-marketplace?period=30days
```

**Response:**
```json
[
  {
    "marketplace": "wildberries",
    "account_name": "Мой магазин WB",
    "total_quantity": 789,
    "total_revenue": 234567.89,
    "order_count": 345
  },
  {
    "marketplace": "ozon",
    "account_name": "Мой магазин Ozon",
    "total_quantity": 456,
    "total_revenue": 123456.78,
    "order_count": 234
  }
]
```

---

### Get Product Performance

Детальная аналитика по конкретному товару:

```http
GET /api/analytics/product/123/performance?period=30days
```

**Response:**
```json
{
  "total_quantity": 450,
  "total_revenue": 45000.00,
  "order_count": 234,
  "avg_price": 100.00,
  "sales_by_day": [
    {"date": "2025-12-12", "quantity": 15},
    {"date": "2025-12-13", "quantity": 18},
    ...
  ]
}
```

---

## Визуализации

### 1. График продаж по дням

**Тип:** Line Chart (линейный)
**Библиотека:** Chart.js 4.4.0
**Данные:** Выручка по дням
**Интерактивность:** Hover tooltips с форматированием

**Использование:**
- Выявление трендов
- Определение пиков продаж
- Сезонный анализ

**Пример интеграции:**
```javascript
new Chart(ctx, {
    type: 'line',
    data: {
        labels: salesByDay.labels,
        datasets: [{
            label: 'Выручка (₽)',
            data: salesByDay.revenues,
            borderColor: 'rgb(99, 102, 241)',
            tension: 0.3,
            fill: true
        }]
    }
});
```

---

### 2. Диаграмма по категориям

**Тип:** Doughnut Chart (кольцевая)
**Данные:** Выручка по категориям
**Цвета:** 6 предустановленных цветов

**Использование:**
- Определение популярных категорий
- Балансировка ассортимента
- Планирование закупок

---

## Периоды анализа

### Стандартные периоды

| Период   | Описание                     | Use Case                       |
|----------|------------------------------|--------------------------------|
| today    | Текущий день                 | Операционный контроль          |
| 7days    | Последние 7 дней             | Недельный анализ               |
| 30days   | Последние 30 дней            | Месячная аналитика (default)   |
| 90days   | Последние 90 дней            | Квартальный обзор              |

### Расчет предыдущего периода

Для сравнения Growth % используется предыдущий период той же длительности:

**Пример:**
- Текущий: 2025-12-12 до 2026-01-11 (30 дней)
- Предыдущий: 2025-11-12 до 2025-12-12 (30 дней)

---

## Top Products Analysis

### Критерии ранжирования

Товары ранжируются по **общей выручке** (revenue) за период.

**ТОП-10 метрики:**
1. Название товара
2. SKU
3. Продано единиц
4. Выручка (₽)
5. Количество заказов
6. Средняя цена

### Использование данных

**Для ТОПа:**
- ✅ Увеличить закупки
- ✅ Расширить ассортимент похожих товаров
- ✅ Оптимизировать рекламу на эти товары
- ✅ Анализировать что делает их популярными

**Для ФЛОПа:**
- ⚠️ Снизить цены (акции)
- ⚠️ Улучшить описания/фото
- ⚠️ Проверить качество
- ⚠️ Рассмотреть снятие с продажи

---

## Интеграция в приложение

### Frontend Implementation

**Alpine.js Component:**

```javascript
function analyticsPage() {
    return {
        period: '30days',
        overview: {},

        async loadData() {
            const response = await fetch(`/api/analytics/dashboard?period=${this.period}`);
            const data = await response.json();

            this.overview = data.overview;
            // Render charts...
        }
    };
}
```

**Chart.js Integration:**

```html
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
```

---

## Performance Optimization

### Database Indexing

Для быстрых запросов убедитесь в наличии индексов:

```sql
-- marketplace_orders
CREATE INDEX idx_orders_created_at ON marketplace_orders(created_at);
CREATE INDEX idx_orders_account_id ON marketplace_orders(marketplace_account_id);

-- marketplace_order_items
CREATE INDEX idx_items_order_id ON marketplace_order_items(order_id);
CREATE INDEX idx_items_variant_id ON marketplace_order_items(product_variant_id);

-- marketplace_accounts
CREATE INDEX idx_accounts_company_id ON marketplace_accounts(company_id);
```

### Caching Strategy

Для больших объемов данных используйте кеширование:

```php
use Illuminate\Support\Facades\Cache;

$overview = Cache::remember(
    "analytics_overview_{$companyId}_{$period}",
    now()->addMinutes(5),
    fn() => $analyticsService->getOverview($companyId, $period)
);
```

---

## Use Cases

### 1. Ежедневный мониторинг

**Цель:** Контроль текущих продаж

**Настройки:**
- Period: `today`
- Проверка: Каждое утро

**Метрики:**
- Выручка vs вчера
- Топ товары дня

---

### 2. Недельный отчет

**Цель:** Операционный анализ

**Настройки:**
- Period: `7days`
- Проверка: Каждый понедельник

**Метрики:**
- Рост выручки %
- Средний чек
- Новые топ товары

---

### 3. Месячная аналитика

**Цель:** Стратегическое планирование

**Настройки:**
- Period: `30days`
- Проверка: В конце месяца

**Метрики:**
- Сравнение с прошлым месяцем
- Анализ категорий
- Планирование закупок

---

### 4. Квартальный обзор

**Цель:** Долгосрочные тренды

**Настройки:**
- Period: `90days`
- Проверка: Раз в квартал

**Метрики:**
- Сезонность
- Рост бизнеса
- Эффективность маркетплейсов

---

## Troubleshooting

### Нет данных на дашборде

**Проблема:** Пустые графики и таблицы

**Решения:**
1. Проверьте наличие заказов за период:
   ```sql
   SELECT COUNT(*) FROM marketplace_orders
   WHERE created_at >= NOW() - INTERVAL 30 DAY;
   ```

2. Проверьте связь company_id:
   ```php
   $user = Auth::user();
   $companyId = $user->companies()->first()?->id;
   ```

3. Убедитесь что order_items существуют:
   ```sql
   SELECT COUNT(*) FROM marketplace_order_items;
   ```

---

### Медленная загрузка

**Проблема:** Дашборд долго грузится (>5 сек)

**Решения:**
1. Добавьте индексы (см. Performance Optimization)
2. Используйте кеширование
3. Уменьшите период (90days → 30days)
4. Оптимизируйте запросы:
   ```php
   // Используйте select только нужных полей
   ->select(['id', 'name', 'price'])
   ```

---

### Неправильные суммы

**Проблема:** Метрики не сходятся

**Проверки:**
1. Формат дат в БД (timezone)
2. Фильтрация по company_id
3. Статусы заказов (включаются ли отмененные?)

---

## Best Practices

### 1. Регулярный анализ

Установите routine:
- **Ежедневно:** Быстрый взгляд на `today`
- **Еженедельно:** Детальный анализ `7days`
- **Ежемесячно:** Стратегический обзор `30days`

### 2. Действия на основе данных

**При росте выручки:**
- Анализируйте ТОП товары
- Закупайте больше популярных позиций
- Масштабируйте успешные категории

**При падении:**
- Запускайте акции
- Оптимизируйте цены
- Улучшайте карточки товаров

### 3. Сравнение периодов

Всегда смотрите на Growth %:
- Положительный — продолжайте стратегию
- Отрицательный — ищите причины и меняйте подход

---

## Roadmap

**Planned Features:**

- 📊 Прогнозирование продаж (ML)
- 📈 Детальная когортная аналитика
- 💰 ROI по товарам и категориям
- 🎯 Custom date ranges
- 📧 Email отчеты
- 📱 Export в Excel/PDF
- 🔔 Alerts на аномалии
- 🤖 AI рекомендации

---

## Support

- Email: [support@sellermind.ai](mailto:support@sellermind.ai)
- Telegram: [@sellermind_support](https://t.me/sellermind_support)

---

**Last Updated:** 2026-01-11
**Version:** 1.0
**Maintained by:** SellerMind AI Team
