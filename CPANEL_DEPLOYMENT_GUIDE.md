# 🚀 Руководство по развертыванию SellerMind AI на cPanel

## Содержание

1. [Введение](#введение)
2. [Требования](#требования)
3. [Подготовка локального окружения](#подготовка-локального-окружения)
4. [Настройка cPanel](#настройка-cpanel)
5. [Загрузка файлов](#загрузка-файлов)
6. [Установка и конфигурация](#установка-и-конфигурация)
7. [Настройка Cron задач](#настройка-cron-задач)
8. [SSL сертификат](#ssl-сертификат)
9. [Проверка работоспособности](#проверка-работоспособности)
10. [Troubleshooting](#troubleshooting)

---

## Введение

Это руководство описывает процесс развертывания Laravel приложения **SellerMind AI** на хостинге с cPanel. 

**Примерное время развертывания:** 2-4 часа

> [!IMPORTANT]
> Убедитесь, что у вас есть:
> - SSH доступ к серверу (желательно)
> - Доступ к cPanel
> - API ключи для маркетплейсов
> - SMTP настройки для почты

---

## Требования

### Серверные требования

- **PHP**: 8.2 или выше
- **MySQL**: 8.0 или выше
- **Доступное место**: минимум 500MB
- **RAM**: минимум 512MB (рекомендуется 1GB+)
- **SSH доступ**: желательно

### PHP Расширения

Убедитесь, что установлены следующие расширения:

- `pdo_mysql`
- `mbstring`
- `xml`
- `curl`
- `zip`
- `gd`
- `bcmath`
- `json`
- `redis` (опционально, но рекомендуется)

### Проверка в cPanel

1. Откройте **cPanel** → **Select PHP Version**
2. Убедитесь, что выбран PHP 8.2 или выше
3. Проверьте установленные расширения (Extensions)

---

## Подготовка локального окружения

### Шаг 1: Настройка .env файла

1. Откройте файл `.env.cpanel` в корне проекта
2. Заполните все необходимые значения:

```bash
# Ваш домен
APP_URL=https://your-domain.com

# База данных (формат cPanel: username_dbname)
DB_DATABASE=username_sellermind
DB_USERNAME=username_seller
DB_PASSWORD=ваш_пароль

# API ключи маркетплейсов
# (см. документацию каждого маркетплейса)
```

> [!WARNING]
> Не забудьте установить:
> - `APP_DEBUG=false`
> - `APP_ENV=production`

### Шаг 2: Создание пакета для развертывания

Выполните скрипт для подготовки архива:

```bash
cd /Applications/MAMP/htdocs/sellermind-ai
bash deploy-cpanel-package.sh
```

Этот скрипт:
- ✅ Соберет frontend ассеты (`npm run build`)
- ✅ Установит production зависимости Composer
- ✅ Создаст ZIP архив с нужными файлами
- ✅ Исключит ненужные файлы (node_modules, .git, tests)

После выполнения у вас появится файл: `sellermind-ai-cpanel-YYYYMMDD-HHMMSS.zip`

---

## Настройка cPanel

### Шаг 1: Создание базы данных MySQL

1. Откройте **cPanel** → **MySQL Database Wizard**
2. **Шаг 1**: Введите имя БД: `sellermind`
   - cPanel автоматически добавит префикс: `username_sellermind`
3. **Шаг 2**: Создайте пользователя:
   - Username: `seller`
   - Password: сгенерируйте надежный пароль (сохраните его!)
4. **Шаг 3**: Назначьте права: **ALL PRIVILEGES**
5. **Завершить**

> [!NOTE]
> Запомните или запишите:
> - Имя БД: `username_sellermind`
> - Пользователь: `username_seller`
> - Пароль

### Шаг 2: Настройка PHP

1. Откройте **cPanel** → **MultiPHP Manager**
2. Выберите ваш домен
3. Установите PHP версию: **8.2** или **8.3**
4. Сохраните

Затем настройте параметры PHP:

1. Откройте **cPanel** → **MultiPHP INI Editor**
2. Выберите ваш домен
3. Установите следующие значения:

```ini
memory_limit = 256M
max_execution_time = 300
upload_max_filesize = 20M
post_max_size = 20M
max_input_vars = 3000
```

4. Сохраните изменения

---

## Загрузка файлов

Есть два способа загрузки файлов на сервер:

### Способ 1: File Manager (проще)

1. Откройте **cPanel** → **File Manager**
2. Перейдите в домашнюю директорию (`/home/username`)
3. Нажмите **Upload**
4. Выберите `sellermind-ai-cpanel-*.zip`
5. Дождитесь завершения загрузки
6. Кликните правой кнопкой на архив → **Extract**
7. Распакуйте в `/home/username/sellermind-ai`

### Способ 2: FTP (быстрее для больших файлов)

1. Откройте **cPanel** → **FTP Accounts**
2. Используйте данные для подключения через FileZilla или другой FTP клиент
3. Загрузите архив в `/home/username`
4. Используйте File Manager для распаковки

---

## Установка и конфигурация

### Шаг 1: Подключение по SSH

Подключитесь к серверу через SSH:

```bash
ssh username@your-server.com
```

Введите пароль от cPanel аккаунта.

> [!TIP]
> Если SSH недоступен, используйте **Terminal** в cPanel (если доступен), или выполняйте команды по одной через **Cron Jobs**.

### Шаг 2: Переход в директорию проекта

```bash
cd ~/sellermind-ai
```

### Шаг 3: Настройка .env

1. Переименуйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

2. Отредактируйте `.env`:

```bash
nano .env
```

или через **File Manager**: найдите `.env`, кликните **Edit**.

3. Заполните все значения (DB_*, APP_URL, API ключи)
4. Сохраните (Ctrl+O, Enter, Ctrl+X в nano)

### Шаг 4: Выполнение скрипта развертывания

Запустите автоматизированный скрипт:

```bash
bash cpanel-deploy-commands.sh
```

Этот скрипт выполнит:
- ✅ Проверку окружения и конфигурации
- ✅ Проверку версии PHP
- ✅ Установку Composer зависимостей
- ✅ Генерацию APP_KEY
- ✅ Применение миграций БД
- ✅ Синхронизацию ProductVariants
- ✅ Оптимизацию и кэширование
- ✅ Установку прав доступа
- ✅ Создание storage link

> [!CAUTION]
> Если скрипт сообщает об ошибках, прочитайте сообщение и устраните проблему перед продолжением.

### Шаг 5: Настройка симлинка для public_html

```bash
bash setup-symlink.sh
```

Этот скрипт:
- Создаст резервную копию текущего `public_html` (если существует)
- Создаст симлинк: `public_html` → `sellermind-ai/public`

---

## Настройка Cron задач

Laravel требует регулярного выполнения задач через Cron.

### Открытие Cron Jobs в cPanel

1. Откройте **cPanel** → **Cron Jobs**
2. В разделе **Add New Cron Job**

### Задача 1: Laravel Scheduler

**Описание:** Запускает все запланированные задачи Laravel

- **Common Settings**: `* * * * *` (каждую минуту)
- **Command**:
```bash
cd /home/username/sellermind-ai && /usr/bin/php artisan schedule:run >> /dev/null 2>&1
```

> [!IMPORTANT]
> Замените `username` на ваше имя пользователя cPanel!

### Задача 2: Queue Worker

**Описание:** Обрабатывает очередь задач (синхронизация с маркетплейсами)

- **Common Settings**: `* * * * *` (каждую минуту)
- **Command**:
```bash
cd /home/username/sellermind-ai && /usr/bin/php artisan queue:work --stop-when-empty --max-time=3600 >> /home/username/sellermind-ai/storage/logs/queue.log 2>&1
```

> [!NOTE]
> Параметр `--stop-when-empty` заставляет worker завершиться после обработки всех задач. Это необходимо для Cron, т.к. Supervisor обычно недоступен в cPanel.

### Проверка путей

Убедитесь, что используете правильный путь к PHP:

```bash
which php
```

Обычно это `/usr/bin/php` или `/usr/local/bin/php`.

---

## SSL сертификат

### Способ 1: AutoSSL (автоматический, бесплатный)

1. Откройте **cPanel** → **SSL/TLS Status**
2. Найдите ваш домен
3. Нажмите **Run AutoSSL**
4. Дождитесь завершения (обычно 1-2 минуты)

### Способ 2: Let's Encrypt через SSH

Если AutoSSL недоступен:

```bash
# Обычно Let's Encrypt уже интегрирован в cPanel
# Используйте интерфейс cPanel → SSL/TLS → Manage SSL Sites
```

### Проверка SSL

После установки:

1. Откройте `https://your-domain.com` в браузере
2. Проверьте иконку замка в адресной строке
3. Сертификат должен быть валидным

### Настройка принудительного HTTPS

Создайте/отредактируйте `.htaccess` в `public/`:

```apache
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Редирект на HTTPS
    RewriteCond %{HTTPS} off
    RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [L,R=301]
    
    # Laravel rewrite rules
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
```

---

## Проверка работоспособности

### 1. Главная страница

Откройте `https://your-domain.com` в браузере.

**Ожидаемый результат:** Загружается интерфейс SellerMind AI

**Если не работает:**
- Проверьте симлинк: `ls -la ~/public_html`
- Проверьте логи: `tail -50 ~/sellermind-ai/storage/logs/laravel.log`

### 2. Подключение к БД

Выполните через SSH:

```bash
cd ~/sellermind-ai
php artisan tinker --execute="DB::connection()->getPdo(); echo 'DB OK';"
```

**Ожидаемый результат:** `DB OK`

### 3. Статус миграций

```bash
php artisan migrate:status
```

**Ожидаемый результат:** Все миграции в статусе `Ran`

### 4. Проверка Cron задач

Подождите 2-3 минуты после настройки Cron, затем:

```bash
cat ~/sellermind-ai/storage/logs/laravel.log | grep "schedule:run"
```

**Ожидаемый результат:** Есть записи о выполнении scheduler

### 5. Проверка очереди

```bash
tail -20 ~/sellermind-ai/storage/logs/queue.log
```

**Ожидаемый результат:** Логи обработки задач (или пусто, если задач нет)

### 6. Авторизация

1. Перейдите на `/login`
2. Попробуйте войти
3. Проверьте, что сессия сохраняется

---

## Troubleshooting

### Проблема: 500 Internal Server Error

**Симптомы:** Белая страница или "500 Internal Server Error"

**Решение:**

1. Проверьте логи:
```bash
tail -50 ~/sellermind-ai/storage/logs/laravel.log
```

2. Проверьте права доступа:
```bash
chmod -R 755 ~/sellermind-ai/storage
chmod -R 755 ~/sellermind-ai/bootstrap/cache
chmod 644 ~/sellermind-ai/.env
```

3. Очистите кэш:
```bash
cd ~/sellermind-ai
php artisan cache:clear
php artisan config:clear
php artisan view:clear
```

4. Проверьте `.htaccess` в `public/`

---

### Проблема: Database connection failed

**Симптомы:** Ошибка подключения к БД

**Решение:**

1. Проверьте настройки в `.env`:
```bash
grep -E "DB_" ~/sellermind-ai/.env
```

2. Убедитесь, что:
   - `DB_HOST=localhost` (или 127.0.0.1)
   - `DB_DATABASE` имеет префикс username (например, `user123_sellermind`)
   - `DB_USERNAME` имеет префикс username
   - Пароль введен правильно

3. Проверьте существование БД в **cPanel** → **MySQL Databases**

4. Проверьте права пользователя БД

---

### Проблема: Cron задачи не выполняются

**Симптомы:** Нет записей в логах о выполнении scheduler/queue

**Решение:**

1. Проверьте список Cron задач:
   - **cPanel** → **Cron Jobs**
   - Убедитесь, что задачи добавлены

2. Проверьте путь к PHP:
```bash
which php
```

3. Замените `/usr/bin/php` в Cron командах на правильный путь

4. Проверьте логи Cron (обычно в `/var/log/cron` или через cPanel)

5. Тестовая команда:
```bash
cd ~/sellermind-ai && php artisan schedule:run
```

---

### Проблема: Queue задачи не обрабатываются

**Симптомы:** Синхронизация с маркетплейсами не работает

**Решение:**

1. Проверьте настройку `QUEUE_CONNECTION` в `.env`:
```bash
grep QUEUE_CONNECTION ~/sellermind-ai/.env
```

Должно быть `database` (если Redis недоступен) или `redis`.

2. Если используете `database`, убедитесь, что таблица `jobs` существует:
```bash
cd ~/sellermind-ai
php artisan migrate
```

3. Проверьте логи queue:
```bash
tail -50 ~/sellermind-ai/storage/logs/queue.log
```

4. Запустите worker вручную для теста:
```bash
php artisan queue:work --once
```

---

### Проблема: CSS/JS не загружаются

**Симптомы:** Сайт без стилей

**Решение:**

1. Убедитесь, что frontend был собран:
```bash
ls -la ~/sellermind-ai/public/build
```

Должны быть файлы `manifest.json` и директории `assets/`.

2. Если файлов нет, пересоберите локально и загрузите заново.

3. Проверьте `APP_URL` в `.env` - он должен совпадать с доменом

4. Очистите кэш браузера (Ctrl+Shift+R)

---

### Проблема: Превышен memory_limit

**Симптомы:** Ошибка "Allowed memory size exhausted"

**Решение:**

1. Увеличьте `memory_limit` в **cPanel** → **MultiPHP INI Editor**:
```ini
memory_limit = 512M
```

2. Или создайте `.user.ini` в корне проекта:
```ini
memory_limit = 512M
```

3. Перезапустите PHP-FPM (если доступно) или подождите 5 минут

---

### Проблема: Permission denied

**Симптомы:** Ошибки записи в storage или cache

**Решение:**

```bash
cd ~/sellermind-ai
chmod -R 755 storage
chmod -R 755 bootstrap/cache
chmod 644 .env

# Если проблема сохраняется:
chmod -R 775 storage
chmod -R 775 bootstrap/cache
```

---

## Полезные команды

### Просмотр логов

```bash
# Laravel логи
tail -f ~/sellermind-ai/storage/logs/laravel.log

# Queue логи
tail -f ~/sellermind-ai/storage/logs/queue.log

# Последние 50 строк
tail -50 ~/sellermind-ai/storage/logs/laravel.log
```

### Очистка кэша

```bash
cd ~/sellermind-ai
php artisan cache:clear
php artisan config:clear
php artisan route:clear
php artisan view:clear
```

### Повторное кэширование

```bash
cd ~/sellermind-ai
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

### Проверка конфигурации

```bash
cd ~/sellermind-ai
php artisan config:show app
php artisan config:show database
```

### Список маршрутов

```bash
cd ~/sellermind-ai
php artisan route:list
```

### Запуск миграций

```bash
cd ~/sellermind-ai
php artisan migrate --force
```

### Откат миграций

```bash
cd ~/sellermind-ai
# Откатить последнюю пачку
php artisan migrate:rollback

# Откатить определенное количество
php artisan migrate:rollback --step=2
```

---

## Регулярное обслуживание

### Резервное копирование БД

**Вручную через cPanel:**

1. **cPanel** → **phpMyAdmin**
2. Выберите БД `username_sellermind`
3. **Export** → **Go**

**Через SSH:**

```bash
mysqldump -u username_seller -p username_sellermind > backup_$(date +%Y%m%d).sql
```

> [!TIP]
> Настройте автоматические бэкапы через **cPanel** → **Backup Wizard**

### Мониторинг дискового пространства

```bash
# Использование диска
du -sh ~/sellermind-ai/storage/logs

# Очистка старых логов (опционально)
find ~/sellermind-ai/storage/logs -name "*.log" -mtime +30 -delete
```

### Обновление зависимостей

```bash
cd ~/sellermind-ai
composer update
php artisan migrate --force
php artisan config:cache
php artisan route:cache
```

---

## После успешного развертывания

✅ **Поздравляем! Ваш проект развернут.**

Следующие шаги:

1. **Создайте первого пользователя-администратора**
2. **Подключите аккаунты маркетплейсов** (Wildberries, Ozon, Uzum, Yandex Market)
3. **Запустите первую синхронизацию** товаров и заказов
4. **Настройте уведомления** на email
5. **Проведите тестовую продажу** для проверки функционала
6. **Настройте мониторинг** (UptimeRobot, Pingdom и т.д.)
7. **Добавьте сайт в Google Search Console**

---

## Поддержка

При возникновении проблем:

1. Проверьте раздел [Troubleshooting](#troubleshooting)
2. Изучите логи Laravel
3. Проверьте документацию Laravel: https://laravel.com/docs
4. Обратитесь в поддержку хостинг-провайдера (для вопросов о сервере)

---

**Удачного развертывания! 🚀**
