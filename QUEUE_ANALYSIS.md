# Анализ системы очередей

## Дата: 2025-12-15

## Проблемы, которые были обнаружены

### 1. ❌ TimeoutExceededException - джобы падали из-за таймаута

**Проблема**:
- 59 упавших джоб с ошибкой `TimeoutExceededException`
- Конфиг очереди: `retry_after = 90` секунд
- Джоба `MonitorMarketplaceChangesJob`: `timeout = 300` секунд
- **Конфликт**: `retry_after` переопределял `timeout` джобы

**Решение**:
```php
// config/queue.php
'database' => [
    'retry_after' => (int) env('DB_QUEUE_RETRY_AFTER', 600), // Увеличено с 90 до 600 секунд
],
```

### 2. ⚠️ Накопление джоб мониторинга (43 джобы в очереди)

**Проблема**:
- MonitorMarketplaceChangesJob после выполнения всегда создаёт новую джобу через 60 секунд
- Один воркер не успевает обрабатывать новые джобы
- Результат: накопление очереди

**Причина**:
```php
// MonitorMarketplaceChangesJob::handle()
try {
    $this->checkOrdersChanges($syncService);
    $this->syncSuppliesIfNeeded();
    $this->rescheduleMonitoring(); // ← Создаёт новую джобу
} catch (\Throwable $e) {
    $this->rescheduleMonitoring(); // ← И здесь тоже!
}
```

**Частичное решение**:
Добавлен `uniqueId()` для предотвращения дублирования:
```php
public function uniqueId(): string
{
    return "monitor_marketplace_{$this->account->id}";
}
```

**Примечание**: Laravel 11 поддерживает уникальные джобы через middleware, но это требует Redis/Cache драйвера.

### 3. ⚠️ Только один воркер

**Проблема**:
```bash
ps aux | grep "queue:work" | grep -v grep | wc -l
# Output: 1
```

Один воркер обрабатывает все джобы последовательно.

**Рекомендация**: Запустить несколько воркеров:
```bash
# Вариант 1: Вручную в разных терминалах
php artisan queue:work &
php artisan queue:work &
php artisan queue:work &

# Вариант 2: Использовать Supervisor (рекомендуется для продакшена)
```

### 4. 🔄 Воркер останавливается после queue:restart

**Проблема**:
После выполнения `php artisan queue:restart` воркер завершается.

**Причина**:
Команда `queue:restart` отправляет сигнал воркерам завершиться. Они корректно завершаются, но не перезапускаются автоматически.

**Решение для разработки**:
```bash
# Запуск воркера в фоне
nohup php artisan queue:work --timeout=600 > storage/logs/queue-worker.log 2>&1 &
```

**Решение для продакшена**:
Использовать Supervisor или systemd для автоматического перезапуска.

## Текущее состояние системы

### Конфигурация

```
Connection: database
Driver: database
Table: jobs
Queue: default
Retry after: 600 сек (10 минут)
```

### Статус

- ✅ Упавшие джобы очищены (было 59, стало 0)
- ✅ Таймаут увеличен до 600 секунд
- ✅ Добавлен uniqueId для MonitorMarketplaceChangesJob
- ✅ Воркер запущен с `--timeout=600`
- ✅ Очередь очищена от накопившихся джоб

### Активные аккаунты

```
Всего аккаунтов: 3
Активных: 3

- ID: 1 | Marketplace: uzum | Name: FORRIS
- ID: 2 | Marketplace: wb   | Name: FORRIS HOME
- ID: 6 | Marketplace: uzum | Name: FORRIS HOME UZ
```

## Архитектура MonitorMarketplaceChangesJob

### Что делает джоба

1. **Проверяет изменения в заказах** (`checkOrdersChanges`):
   - Синхронизирует заказы за последний час
   - Сравнивает количество до/после
   - Проверяет изменение `updated_at`
   - Отправляет broadcast событие `MarketplaceDataChanged`

2. **Синхронизирует поставки WB** (`syncSuppliesIfNeeded`):
   - Только для Wildberries аккаунтов
   - Вызывает `SyncWildberriesSupplies` job синхронно
   - Обновляет статусы заказов

3. **Перезапускает мониторинг** (`rescheduleMonitoring`):
   - Создаёт новую джобу с задержкой 60 секунд
   - **Проблема**: создаётся всегда, даже при ошибке

### Время выполнения

```
Джоба мониторинга выполняется долго:
- checkOrdersChanges: ~30-60 секунд
- syncSuppliesIfNeeded: ~60-120 секунд (для WB)
- ИТОГО: 90-180 секунд

С 3 аккаунтами:
- 1 воркер обрабатывает: 270-540 секунд (4.5-9 минут)
- Новые джобы добавляются каждые: 60 секунд

Результат: накопление очереди
```

## Рекомендации

### Немедленные действия (сделано)

1. ✅ Увеличить `retry_after` до 600 секунд
2. ✅ Очистить упавшие джобы: `php artisan queue:flush`
3. ✅ Очистить очередь: `DB::table('jobs')->delete()`
4. ✅ Добавить `uniqueId()` в MonitorMarketplaceChangesJob
5. ✅ Запустить воркер с `--timeout=600`

### Краткосрочные действия (рекомендуется)

1. **Запустить 2-3 воркера** для параллельной обработки:
   ```bash
   php artisan queue:work --timeout=600 &
   php artisan queue:work --timeout=600 &
   php artisan queue:work --timeout=600 &
   ```

2. **Настроить Supervisor** для автоматического перезапуска воркеров:
   ```ini
   [program:sellermind-queue]
   process_name=%(program_name)s_%(process_num)02d
   command=php /path/to/artisan queue:work --sleep=3 --tries=3 --timeout=600
   autostart=true
   autorestart=true
   numprocs=3
   redirect_stderr=true
   stdout_logfile=/path/to/logs/queue-worker.log
   ```

3. **Мониторинг очереди**:
   ```bash
   # Проверка статуса
   php test-queue-status.php

   # Мониторинг логов
   tail -f storage/logs/laravel.log | grep -i "monitoring\|error"
   ```

### Долгосрочные действия

1. **Разделить джобы**:
   ```php
   // Вместо одной тяжелой джобы:
   MonitorMarketplaceChangesJob

   // Создать несколько легких:
   - CheckOrdersChangesJob (только проверка)
   - SyncMarketplaceSuppliesJob (только синхронизация)
   ```

2. **Использовать Laravel Horizon** (для Redis):
   - Лучший мониторинг очередей
   - Автоматическое балансирование
   - Уникальные джобы из коробки

3. **Оптимизировать синхронизацию**:
   ```php
   // Не синхронизировать всё каждый раз
   // Синхронизировать только изменения за последние N минут
   $syncService->syncOrders(
       $this->account,
       Carbon::now()->subMinutes(10), // Вместо subHour()
       Carbon::now()
   );
   ```

4. **Добавить rate limiting**:
   ```php
   // Не запускать мониторинг чаще чем раз в 2 минуты
   if (Cache::has("monitor_running_{$this->account->id}")) {
       return; // Пропускаем, если уже выполняется
   }

   Cache::put("monitor_running_{$this->account->id}", true, 120);
   ```

## Файлы изменений

### Изменённые файлы

1. **config/queue.php**:
   - Увеличен `retry_after` с 90 до 600 секунд

2. **app/Jobs/Marketplace/MonitorMarketplaceChangesJob.php**:
   - Добавлен метод `uniqueId()`

### Созданные файлы

1. **test-queue-status.php** - скрипт для проверки статуса очередей
2. **test-start-monitoring.php** - скрипт для запуска мониторинга
3. **QUEUE_ANALYSIS.md** - этот документ

## Примеры использования

### Проверить статус очередей
```bash
php test-queue-status.php
```

### Запустить мониторинг
```bash
php test-start-monitoring.php
```

### Очистить упавшие джобы
```bash
php artisan queue:flush
```

### Очистить всю очередь
```bash
php artisan tinker --execute="DB::table('jobs')->delete()"
```

### Перезапустить воркеры
```bash
php artisan queue:restart
```

### Запустить воркер
```bash
# Передний план (для тестирования)
php artisan queue:work --timeout=600

# Фоновый режим (для разработки)
nohup php artisan queue:work --timeout=600 > storage/logs/queue-worker.log 2>&1 &
```

## Итоги

### Решено проблем: 4

1. ✅ TimeoutExceededException - увеличен таймаут
2. ✅ 59 упавших джоб - очищены
3. ✅ 43 накопившихся джобы - очищены
4. ✅ Воркер не запущен - запущен

### Требует внимания:

1. ⚠️ Настроить Supervisor для автоматического перезапуска воркеров
2. ⚠️ Запустить несколько воркеров для параллельной обработки
3. ⚠️ Оптимизировать MonitorMarketplaceChangesJob (разделить на части)
4. ⚠️ Добавить rate limiting для мониторинга

### Статус: ✅ Система работает, но требует оптимизации

---

**Последнее обновление**: 2025-12-15
**Автор**: Claude Code
