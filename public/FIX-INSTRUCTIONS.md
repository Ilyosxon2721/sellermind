# 🛠️ Инструкция по исправлению платформы SellerMind AI

## ⚠️ Проблема
Платформа не работает из-за повреждённого localStorage, который блокирует Alpine.js и авторизацию.

---

## ✅ Быстрое решение (5 минут)

### Шаг 1: Откройте диагностический инструмент
```
http://127.0.0.1:8000/diagnostic.html
```

### Шаг 2: Проверьте localStorage
1. Нажмите кнопку **"Проверить localStorage"**
2. Если видите красные метки "JSON Parse Error" - переходите к шагу 3
3. Если всё зелёное - переходите к шагу 4

### Шаг 3: Очистите localStorage
1. Нажмите красную кнопку **"Очистить и исправить"**
2. Подтвердите действие (это удалит все данные и потребует повторной авторизации)
3. После очистки увидите зелёное сообщение "localStorage очищен"

### Шаг 4: Войдите заново
1. На странице diagnostic.html найдите секцию "3. Authentication Test"
2. Введите свои данные:
   - Email: `admin@sellermind.ai`
   - Password: `password`
3. Нажмите **"Тест авторизации"**
4. Должны увидеть зелёное сообщение "Авторизация успешна!"

### Шаг 5: Запустите полную диагностику
1. Прокрутите вниз до фиолетовой секции "Полная диагностика"
2. Нажмите **"Запустить все тесты"**
3. Убедитесь, что все 5 секций показывают статус "OK" или "Success":
   - ✅ LocalStorage Status
   - ✅ API Health Check
   - ✅ Authentication Test
   - ✅ Database & Routes
   - ✅ Alpine.js Status

---

## 🎯 Проверка работоспособности

### Тест 1: Авторизация
1. Откройте http://127.0.0.1:8000/login
2. Войдите с вашими данными
3. Должна открыться страница `/chat`
4. **НЕ должно быть ошибок в консоли браузера** (F12 → Console)

### Тест 2: Маркетплейсы
1. Откройте http://127.0.0.1:8000/marketplace
2. Должны увидеть список подключённых маркетплейсов
3. Если маркетплейсов нет - это нормально, значит их нужно добавить

### Тест 3: Wildberries Settings
1. В списке маркетплейсов найдите Wildberries аккаунт
2. Нажмите на него (откроется /marketplace/2)
3. Нажмите на фиолетовую карточку **"WB Settings"**
4. Должна открыться страница настроек токенов WB
5. Добавьте ваш реальный токен и нажмите "Сохранить"
6. Нажмите "Проверить подключение"

---

## 🚨 Если что-то пошло не так

### Проблема: "Всё ещё вижу ошибки в консоли"
**Решение:**
```javascript
// Откройте консоль браузера (F12) и выполните:
localStorage.clear();
location.reload();
```

### Проблема: "Нет подключённых маркетплейсов"
**Решение:**
Проверьте, что WB аккаунт принадлежит правильной компании:
```bash
php artisan tinker
$account = App\Models\MarketplaceAccount::find(2);
echo "Company ID: " . $account->company_id . "\n";
$user = App\Models\User::where('email', 'admin@sellermind.ai')->first();
echo "User's company IDs: " . $user->companies->pluck('id') . "\n";

# Если company_id не совпадает, переместите:
$account->update(['company_id' => 2]); // или другой ID вашей компании
```

### Проблема: "401 Unauthorized при запросах"
**Решение:**
1. Очистите localStorage (см. выше)
2. Заново авторизуйтесь
3. Проверьте, что токен сохранился:
```javascript
// В консоли браузера:
console.log('Token:', localStorage.getItem('_x_auth_token'));
```

### Проблема: "Alpine.js не загружается"
**Решение:**
```bash
# Пересоберите frontend:
npm run build
```

---

## 🔍 Дополнительная диагностика

### Проверка API напрямую
```bash
# Health check
curl http://127.0.0.1:8000/api/health

# Должен вернуть:
# {"status":"ok","timestamp":"...","database":"sellermind_ai"}
```

### Проверка маршрутов
```bash
php artisan route:list | grep marketplace
```

### Проверка базы данных
```bash
php artisan tinker
# Проверка WB аккаунтов:
App\Models\MarketplaceAccount::where('marketplace', 'wb')->get(['id', 'company_id', 'name']);

# Проверка компаний:
App\Models\Company::all(['id', 'name']);

# Проверка пользователей:
App\Models\User::with('companies')->get(['id', 'email']);
```

---

## 📋 Что было исправлено

### 1. ✅ Добавлен health endpoint
- URL: `/api/health`
- Проверяет работу API и базы данных

### 2. ✅ Улучшен app.js
- Добавлена автоматическая очистка повреждённых ключей localStorage
- Добавлена функция `safePersist()` с обработкой ошибок
- Теперь Alpine.js не упадёт при повреждении localStorage

### 3. ✅ Создан diagnostic.html
- Комплексная диагностика всех компонентов
- Кнопка "Очистить и исправить" для localStorage
- Тесты авторизации, API, БД, Alpine.js

### 4. ✅ Исправлены токены в wb-settings.blade.php
- Правильная проверка ключей localStorage
- Fallback на старые ключи для обратной совместимости

### 5. ✅ Переписаны миграции WB
- Короткие имена индексов (< 64 символов)
- Все таблицы успешно созданы

---

## 📚 Полезные ссылки

- **Диагностика:** http://127.0.0.1:8000/diagnostic.html
- **Тест авторизации:** http://127.0.0.1:8000/test-auth.html
- **Тест WB API:** http://127.0.0.1:8000/test-wb-api.html
- **Health Check:** http://127.0.0.1:8000/api/health
- **Отчёт об ошибках:** /public/error-report.md

---

## ⚡ Быстрый чек-лист

- [ ] Открыл diagnostic.html
- [ ] Проверил localStorage
- [ ] Очистил localStorage (если были ошибки)
- [ ] Заново авторизовался
- [ ] Запустил полную диагностику
- [ ] Все тесты показывают OK/Success
- [ ] Зашёл на /login без ошибок
- [ ] Зашёл на /marketplace без ошибок
- [ ] Открыл WB Settings без ошибок
- [ ] Добавил WB токен
- [ ] Проверил подключение к WB API

---

## 💡 Профилактика на будущее

1. **Регулярно проверяйте diagnostic.html** после деплоя
2. **Следите за консолью браузера** - ошибки там видны сразу
3. **Не закрывайте браузер аварийно** - это может повредить localStorage
4. **Используйте /api/health** для мониторинга

---

**Если после всех шагов проблема остаётся - откройте /public/error-report.md для детального анализа.**
