<?php

return [
    /*
    |--------------------------------------------------------------------------
    | AI Provider Configuration
    |--------------------------------------------------------------------------
    |
    | Настройки для AI провайдеров (OpenAI, Anthropic).
    | Основной провайдер используется по умолчанию.
    | При ошибках автоматически переключается на резервный.
    |
    */

    'provider' => env('AI_PROVIDER', 'openai'), // openai | anthropic

    'fallback_provider' => env('AI_FALLBACK_PROVIDER', 'anthropic'), // openai | anthropic

    /*
    |--------------------------------------------------------------------------
    | Retry Settings
    |--------------------------------------------------------------------------
    */

    'retry' => [
        'max_attempts' => env('AI_RETRY_ATTEMPTS', 2),
        'delay_ms' => env('AI_RETRY_DELAY', 1000),
    ],

    /*
    |--------------------------------------------------------------------------
    | Timeout Settings
    |--------------------------------------------------------------------------
    */

    'timeout' => [
        'default' => 60,
        'kpi' => 90,
    ],
];
