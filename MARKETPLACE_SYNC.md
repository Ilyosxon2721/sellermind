# Автоматическая синхронизация заказов с маркетплейсов

## 📋 Обзор

Система автоматически синхронизирует заказы из Wildberries, Uzum, Ozon и других маркетплейсов каждые 10 минут.

## 🚀 Запуск синхронизации

### Ручной запуск

```bash
# Синхронизация всех активных аккаунтов
php artisan marketplace:sync-orders

# Синхронизация конкретного аккаунта
php artisan marketplace:sync-orders --account=2

# Синхронизация за последние 30 дней
php artisan marketplace:sync-orders --days=30
```

### Автоматическая синхронизация (рекомендуется)

#### Development (локальная разработка)

Запустите scheduler в отдельном терминале:

```bash
php artisan schedule:work
```

Это запустит Laravel Scheduler который будет проверять расписание каждую минуту и выполнять задачи по расписанию.

#### Production (сервер)

Добавьте cron задачу (от имени веб-сервера, обычно `www-data` или `nginx`):

```bash
# Откройте crontab
crontab -e

# Добавьте строку:
* * * * * cd /path/to/sellermind-ai && php artisan schedule:run >> /dev/null 2>&1
```

**Важно**: Замените `/path/to/sellermind-ai` на реальный путь к проекту.

## ⚙️ Настройки расписания

Расписание настраивается в файле `routes/console.php`:

```php
// Синхронизация заказов каждые 10 минут
Schedule::command('marketplace:sync-orders --days=7')
    ->everyTenMinutes()
    ->withoutOverlapping(10)
    ->appendOutputTo(storage_path('logs/marketplace-sync.log'));
```

### Доступные интервалы

- `->everyMinute()` - каждую минуту
- `->everyFiveMinutes()` - каждые 5 минут
- `->everyTenMinutes()` - каждые 10 минут (текущая настройка)
- `->everyFifteenMinutes()` - каждые 15 минут
- `->everyThirtyMinutes()` - каждые 30 минут
- `->hourly()` - каждый час
- `->daily()` - каждый день

## 📊 Просмотр расписания

Просмотр всех запланированных задач:

```bash
php artisan schedule:list
```

Пример вывода:
```
*/10 * * * *  php artisan marketplace:sync-orders --days=7  Next Due: через 8 минут
```

## 📝 Логи синхронизации

Логи сохраняются в:
- `storage/logs/marketplace-sync.log` - логи синхронизации
- `storage/logs/laravel.log` - общие логи приложения

Просмотр логов:

```bash
# Последние 50 строк
tail -n 50 storage/logs/marketplace-sync.log

# Мониторинг в реальном времени
tail -f storage/logs/marketplace-sync.log
```

## 🔍 Как это работает

### Wildberries (FBS модель)

1. **Новые заказы**: Получаются через `/api/v3/orders/new` (статус: `new`)
2. **Заказы в сборке**: Получаются через `/api/v3/supplies/{id}/orders` (статус: `confirm`)
3. **Обновление статусов**: Дополнительный запрос через `/api/v3/orders/status`

### Важно понимать

- **FBS API не поддерживает фильтрацию по датам** для endpoint `/api/v3/orders`
- Новые заказы доступны только пока они в статусе `new`
- Заказы в сборке (`confirm`) находятся в поставках
- Завершенные/отмененные заказы не возвращаются через API

### Uzum, Ozon, Yandex Market

Синхронизация через соответствующие API endpoints с поддержкой фильтрации по датам.

## 🛠️ Решение проблем

### Синхронизация не запускается

1. Проверьте что scheduler запущен:
   ```bash
   php artisan schedule:work
   ```

2. Проверьте cron (на production):
   ```bash
   crontab -l
   ```

3. Проверьте права на запись логов:
   ```bash
   chmod -R 775 storage/logs
   ```

### Ошибки синхронизации

Проверьте логи:

```bash
tail -n 100 storage/logs/marketplace-sync.log
```

Типичные ошибки:
- **"Не задан shop_id"** - не настроен Shop ID в настройках маркетплейса
- **"Incorrect parameter"** - проблема с токеном или параметрами API
- **"Unauthenticated"** - истек срок действия токена

### Нет новых заказов

1. Проверьте активность аккаунта:
   ```bash
   php artisan tinker
   >>> MarketplaceAccount::where('is_active', true)->get()
   ```

2. Проверьте последнюю синхронизацию:
   ```bash
   php artisan tinker
   >>> MarketplaceSyncLog::orderBy('created_at', 'desc')->limit(10)->get()
   ```

3. Проверьте личный кабинет маркетплейса - возможно заказы уже обработаны

## 📈 Мониторинг

### Просмотр статистики синхронизации

```bash
php artisan tinker
```

```php
// Последние 10 синхронизаций
MarketplaceSyncLog::orderBy('created_at', 'desc')->limit(10)->get();

// Количество заказов по маркетплейсам
DB::table('marketplace_orders')
    ->join('marketplace_accounts', 'marketplace_orders.marketplace_account_id', '=', 'marketplace_accounts.id')
    ->select('marketplace_accounts.marketplace', DB::raw('count(*) as total'))
    ->groupBy('marketplace_accounts.marketplace')
    ->get();

// Новые заказы за последний час
MarketplaceOrder::where('created_at', '>', now()->subHour())->count();
```

## 🔐 Безопасность

- Токены доступа хранятся в зашифрованном виде в БД
- Логи не содержат чувствительных данных (токены, пароли)
- Команды синхронизации не требуют прав администратора

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `storage/logs/marketplace-sync.log`
2. Проверьте статус задач: `php artisan schedule:list`
3. Запустите синхронизацию вручную: `php artisan marketplace:sync-orders`
4. Проверьте настройки токенов в личном кабинете маркетплейса

## 🎯 Рекомендации

### Development
- Используйте `php artisan schedule:work` для тестирования
- Установите интервал побольше (каждые 30 минут) чтобы не перегружать API

### Production
- Настройте cron задачу
- Используйте интервал 10-15 минут для своевременной обработки заказов
- Настройте мониторинг логов (например, через Logrotate)
- Настройте алерты на ошибки синхронизации

## 📚 Дополнительная информация

- [Laravel Task Scheduling](https://laravel.com/docs/11.x/scheduling)
- [Wildberries API Documentation](https://dev.wildberries.ru/en/openapi/orders-fbs)
- [Cron Expression Generator](https://crontab.guru/)
