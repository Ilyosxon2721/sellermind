# PWA (Progressive Web App) Setup Guide

SellerMind теперь является Progressive Web App, что позволяет пользователям устанавливать приложение на свои устройства и работать с ним как с нативным приложением.

## Что такое PWA?

Progressive Web App - это веб-приложение, которое использует современные возможности браузера для предоставления опыта, похожего на нативное приложение:

- 📱 **Установка на главный экран** - без App Store/Google Play
- 🚀 **Быстрая загрузка** - благодаря кешированию
- 📴 **Offline-режим** - базовая работа без интернета
- 🔔 **Push-уведомления** - (готово к внедрению)
- 📲 **Нативный интерфейс** - полноэкранный режим без браузерных элементов

## Что уже настроено

### ✅ 1. Web App Manifest (`/public/manifest.json`)
Содержит метаданные приложения:
- Название, описание, иконки
- Цветовая тема (#2563eb - синий)
- Ориентация экрана (portrait)
- Режим отображения (standalone)

### ✅ 2. Service Worker (`/public/sw.js`)
Обеспечивает:
- Кеширование статических ресурсов
- Network-first стратегия для данных
- Offline-страница при отсутствии сети
- Автоматическое обновление при новой версии
- Push-уведомления (готовы к использованию)

### ✅ 3. Offline Page (`/public/offline.html`)
Красивая страница, которая показывается при отсутствии интернета:
- Информативное сообщение
- Кнопка повторной попытки
- Автоматическая проверка соединения
- Советы по восстановлению связи

### ✅ 4. PWA Meta-теги (`resources/views/layouts/app.blade.php`)
Добавлены мета-теги для:
- iOS (Apple Touch Icons, Web App Capable)
- Android (Theme Color, Manifest)
- Кроссбраузерная совместимость

### ✅ 5. Автоматическая установка
- Кнопка "Установить приложение" появляется автоматически
- Скрывается через 10 секунд если не нажата
- Доступна через `window.installPWA()`

## Что нужно сделать

### 1. Создать иконки приложения

Вам нужно создать иконки разных размеров и поместить их в `/public/images/icons/`:

**Требуемые размеры:**
```
icon-72x72.png
icon-96x96.png
icon-128x128.png
icon-144x144.png
icon-152x152.png
icon-192x192.png
icon-384x384.png
icon-512x512.png
badge-72x72.png (для уведомлений)
```

**Рекомендации по дизайну:**
- Используйте логотип SellerMind
- Синий фон (#2563eb)
- Белая иконка/текст
- Закругленные углы (20% от размера)
- Простой, узнаваемый дизайн

**Быстрый способ создать иконки:**

1. Создайте один SVG или PNG 512x512
2. Используйте онлайн-сервис: https://realfavicongenerator.net
3. Или установите утилиту:
   ```bash
   npm install -g pwa-asset-generator
   pwa-asset-generator logo.svg public/images/icons --icon-only
   ```

### 2. Создать скриншоты (опционально)

Для лучшего представления в магазинах создайте скриншоты:

```
/public/images/screenshots/dashboard.png (1280x720)
/public/images/screenshots/mobile-dashboard.png (750x1334)
```

### 3. Обновить Service Worker версию

При каждом обновлении приложения меняйте версию в `/public/sw.js`:

```javascript
const CACHE_VERSION = 'v1.0.1'; // Увеличьте номер версии
```

Это заставит браузеры обновить кеш.

### 4. Настроить HTTPS

PWA требует HTTPS в production (уже настроено для sellermind.uz).

### 5. Тестирование

#### В Chrome DevTools:
1. Откройте DevTools (F12)
2. Вкладка **Application**
3. Проверьте:
   - **Manifest** - должен загружаться корректно
   - **Service Workers** - должен быть зарегистрирован
   - **Storage** - проверьте Cache Storage

#### Lighthouse Audit:
1. DevTools → Lighthouse
2. Выберите "Progressive Web App"
3. Run audit
4. Цель: 90+ баллов

#### Реальное устройство:
1. Откройте сайт на телефоне
2. Должна появиться подсказка "Добавить на главный экран"
3. После установки проверьте работу в standalone-режиме

## Как использовать

### Для пользователей

**На Android:**
1. Откройте sellermind.uz в Chrome
2. Нажмите меню (⋮) → "Установить приложение"
3. Или нажмите кнопку "Установить приложение" на сайте

**На iOS:**
1. Откройте sellermind.uz в Safari
2. Нажмите кнопку "Поделиться" (□↑)
3. Выберите "На экран «Домой»"

**На Desktop:**
1. Откройте sellermind.uz в Chrome
2. Справа в адресной строке появится иконка установки
3. Или нажмите кнопку "Установить приложение"

### Для разработчиков

**Проверить статус Service Worker:**
```javascript
console.log(window.getWebSocketState());
navigator.serviceWorker.getRegistration().then(reg => console.log(reg));
```

**Принудительно обновить Service Worker:**
```javascript
navigator.serviceWorker.getRegistration().then(reg => reg.update());
```

**Показать кнопку установки вручную:**
```javascript
window.installPWA();
```

**Проверить версию кеша:**
```javascript
caches.keys().then(keys => console.log(keys));
```

**Очистить весь кеш:**
```javascript
caches.keys().then(keys => Promise.all(keys.map(k => caches.delete(k))));
```

## Стратегии кеширования

### Текущая стратегия: Network First

```
1. Пытаемся загрузить из сети
2. Если успешно - сохраняем в кеш
3. Если ошибка - берем из кеша
4. Если нет в кеше - показываем offline.html
```

**Преимущества:**
- Всегда свежие данные при наличии сети
- Работает offline с закешированными данными

**Что НЕ кешируется:**
- `/api/*` - API запросы
- `/app/*` - WebSocket
- POST/PUT/DELETE запросы

## Обновление приложения

Когда вы обновляете код:

1. **Увеличьте версию** в `sw.js`:
   ```javascript
   const CACHE_VERSION = 'v1.0.2';
   ```

2. **Пересоберите фронтенд:**
   ```bash
   npm run build
   ```

3. **Обновление произойдет автоматически:**
   - Service Worker обнаружит новую версию
   - Пользователь увидит prompt: "Доступна новая версия"
   - После подтверждения - автоматический reload

## Push-уведомления (будущий функционал)

Service Worker уже поддерживает push-уведомления. Для активации:

1. Настройте VAPID ключи
2. Добавьте подписку на уведомления
3. Отправляйте уведомления с бэкенда

Код уже готов в `sw.js` (события `push` и `notificationclick`).

## Troubleshooting

### Service Worker не регистрируется

**Проблема:** В консоли нет сообщения "PWA: Service Worker registered"

**Решения:**
1. Проверьте HTTPS (обязательно в production)
2. Проверьте что `/sw.js` доступен
3. Откройте DevTools → Application → Service Workers
4. Проверьте ошибки в консоли

### Manifest не загружается

**Проблема:** DevTools → Application → Manifest показывает ошибки

**Решения:**
1. Проверьте что `/manifest.json` доступен
2. Проверьте JSON синтаксис
3. Убедитесь что иконки существуют
4. Проверьте Content-Type: `application/json`

### Кнопка установки не появляется

**Проблема:** Браузер не показывает prompt для установки

**Причины:**
1. Приложение уже установлено
2. Не все PWA критерии выполнены (проверьте Lighthouse)
3. Браузер не поддерживает (Safari на iOS работает иначе)
4. Пользователь ранее отклонил установку

**Решение:**
- Проверьте в DevTools → Application → Manifest
- Запустите Lighthouse audit
- На iOS используйте Safari Share Sheet

### Старая версия после обновления

**Проблема:** После обновления кода отображается старая версия

**Решения:**
1. Увеличьте CACHE_VERSION в sw.js
2. В DevTools: Application → Service Workers → Unregister
3. Очистите кеш: Application → Storage → Clear site data
4. Hard reload: Ctrl+Shift+R

## Полезные ссылки

- MDN PWA Guide: https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps
- Workbox (Google's PWA toolkit): https://developers.google.com/web/tools/workbox
- PWA Checklist: https://web.dev/pwa-checklist/
- Can I Use Service Workers: https://caniuse.com/serviceworkers

## Метрики производительности

После внедрения PWA ожидаемые улучшения:

- ⚡ **First Contentful Paint**: -30-50%
- 📦 **Page Size**: -40-60% (повторные визиты)
- 🔄 **Cache Hit Rate**: 80-90%
- 📴 **Offline Availability**: 100% (UI)
- 📲 **Install Rate**: 10-20% активных пользователей

## Roadmap

Планируемые улучшения:

- [ ] Push-уведомления о новых заказах
- [ ] Offline-редактирование товаров (sync при подключении)
- [ ] Background sync для загрузки данных
- [ ] Периодическая синхронизация (Background Periodic Sync)
- [ ] Share Target API (делиться товарами из других приложений)
- [ ] Web Share API (делиться из SellerMind)
- [ ] Shortcuts API (быстрые действия на иконке)
