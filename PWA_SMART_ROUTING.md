# PWA Smart Routing - Умная маршрутизация

SellerMind использует умную систему маршрутизации, которая определяет контекст использования приложения и показывает соответствующую стартовую страницу.

## Как это работает

### 📱 Установленное PWA приложение (Standalone)

Когда пользователь **устанавливает SellerMind** как приложение:

```
Открывает приложение → Авторизован? → Да → Dashboard
                                    → Нет → Login
```

**Преимущества:**
- Мгновенный доступ к рабочему пространству
- Нет лишних кликов через лендинг
- Как нативное приложение

### 🌐 Обычный браузер

Когда пользователь **открывает в браузере**:

```
Открывает сайт → Landing Page (маркетинг, цены, регистрация)
```

**Преимущества:**
- Привлечение новых пользователей
- Маркетинговая информация
- Конверсия в регистрацию

## Техническая реализация

### 1. Определение режима (Client-Side)

**JavaScript в `resources/js/pwa.js`:**

```javascript
function detectPWAMode() {
    const isStandalone =
        window.matchMedia('(display-mode: standalone)').matches || // Standard
        window.navigator.standalone === true ||                    // iOS
        document.referrer.includes('android-app://');              // Android

    // Сохраняем в cookie для server-side
    document.cookie = `pwa_installed=${isStandalone}; path=/; max-age=31536000`;

    return isStandalone;
}
```

**Методы определения:**
- `window.matchMedia('(display-mode: standalone)')` - стандартный способ
- `window.navigator.standalone` - iOS Safari
- `document.referrer.includes('android-app://')` - Android Chrome

### 2. Маршрутизация (Server-Side)

**Laravel в `routes/web.php`:**

```php
Route::get('/', function (Request $request) {
    // Проверяем PWA режим из cookie
    $isPWA = $request->cookie('pwa_installed') === 'true';

    // Если PWA и авторизован → Dashboard
    if (auth()->check() && $isPWA) {
        return redirect('/dashboard');
    }

    // Иначе → Landing
    return view('welcome', compact('plans'));
});
```

### 3. Манифест настройка

**vite.config.js:**

```javascript
VitePWA({
    manifest: {
        start_url: '/',  // Важно! Не '/dashboard'
        display: 'standalone'
    }
})
```

## Пользовательские сценарии

### Сценарий 1: Новый пользователь (Browser)

```
1. Открывает sellermind.uz
2. Видит Landing Page
3. Узнает о продукте, ценах
4. Регистрируется
5. Попадает в Dashboard
6. Видит prompt "Установить приложение"
```

### Сценарий 2: Установка PWA

```
1. Нажимает "Установить приложение"
2. Приложение устанавливается
3. JavaScript определяет standalone mode
4. Cookie pwa_installed=true устанавливается
5. При следующем открытии → сразу Dashboard (если авторизован)
```

### Сценарий 3: Постоянный пользователь (PWA)

```
1. Открывает приложение с главного экрана
2. Server проверяет cookie pwa_installed=true
3. Видит что пользователь авторизован
4. Редирект → Dashboard
5. Мгновенный доступ к работе!
```

### Сценарий 4: Разлогинился в PWA

```
1. Открывает приложение
2. Cookie pwa_installed=true есть
3. Но auth()->check() = false
4. Показывает Landing? Нет!
5. Можно сделать редирект на /login
```

**Улучшенная версия:**

```php
Route::get('/', function (Request $request) {
    $isPWA = $request->cookie('pwa_installed') === 'true';

    if ($isPWA) {
        // В PWA режиме
        if (auth()->check()) {
            return redirect('/dashboard');
        } else {
            return redirect('/login');  // Не показываем landing
        }
    }

    // В браузере - landing
    return view('welcome', compact('plans'));
});
```

## Отладка

### Проверить режим в консоли

```javascript
// Standalone mode?
console.log(window.isPWAInstalled);

// Cookie установлен?
console.log(document.cookie);

// Display mode?
console.log(window.matchMedia('(display-mode: standalone)').matches);
```

### Симуляция PWA в DevTools

**Chrome:**
1. F12 → DevTools
2. More tools → Application
3. Manifest → "Add to home screen"
4. Закрыть вкладку
5. Открыть из установленного приложения

**Или в адресной строке:**
```
chrome://apps
```

**Firefox:**
```
about:debugging#/runtime/this-firefox
```

### Тестирование режимов

**Режим браузера:**
```bash
# Удалить cookie
document.cookie = 'pwa_installed=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;'

# Перезагрузить
window.location.reload()
```

**Режим PWA:**
```bash
# Установить cookie
document.cookie = 'pwa_installed=true; path=/; max-age=31536000'

# Перезагрузить
window.location.reload()
```

## Альтернативные подходы

### Подход 1: Client-Side Redirect (быстрее)

Вместо server-side можно делать redirect на клиенте:

```javascript
// В welcome.blade.php
if (window.isPWAInstalled && isAuthenticated) {
    window.location.href = '/dashboard';
}
```

**Плюсы:**
- Быстрее (нет server round-trip)
- Меньше нагрузка на сервер

**Минусы:**
- Flash of content (видно landing на миллисекунду)
- SEO может не понравиться

### Подход 2: Разные манифесты

Создать 2 манифеста:
- `/manifest-pwa.json` - start_url: /dashboard
- `/manifest-web.json` - start_url: /

**Плюсы:**
- Нативное поведение

**Минусы:**
- Сложнее поддерживать
- Нужна логика выбора манифеста

### Подход 3: Headers detection

```php
// В middleware
$userAgent = $request->header('User-Agent');
$isStandalone = str_contains($userAgent, 'standalone');
```

**Плюсы:**
- Не зависит от JavaScript

**Минусы:**
- User-Agent ненадежен
- Легко подделать

## Cookie детали

### Установленный cookie

```
Name: pwa_installed
Value: true | false
Path: /
Max-Age: 31536000 (1 год)
SameSite: Lax
```

### Зачем SameSite=Lax?

- Защита от CSRF
- Cookie передается при навигации
- Не передается при cross-site requests

### Почему Max-Age=1 год?

- Пользователь редко переустанавливает PWA
- Нет смысла спрашивать каждую сессию
- Можно увеличить до 10 лет

## Edge Cases

### Case 1: Удаление PWA

Когда пользователь удаляет приложение с главного экрана:

```javascript
// Не сработает автоматически!
// Cookie останется pwa_installed=true
```

**Решение:** Cookie истечет через год или пользователь очистит.

**Альтернатива:**
```javascript
// Периодически проверять
setInterval(() => {
    if (!window.matchMedia('(display-mode: standalone)').matches) {
        if (document.cookie.includes('pwa_installed=true')) {
            // Обновить cookie
            document.cookie = 'pwa_installed=false; path=/';
        }
    }
}, 60000); // каждую минуту
```

### Case 2: Открыл PWA в браузере

Пользователь может открыть ссылку из PWA в браузере:

```
PWA App → Кликнул ссылку → Открылась в браузере
Cookie: pwa_installed=true
Display mode: browser
```

**Поведение:** Покажет Landing (правильно!)

### Case 3: Разные браузеры

Cookie не синхронизируются между браузерами:

```
Chrome: pwa_installed=true
Safari: pwa_installed=false
```

**Поведение:** В каждом браузере свой режим (правильно!)

## Best Practices

### ✅ Do

1. **Используйте cookie + display-mode** (двойная проверка)
2. **Логируйте режим в console** для отладки
3. **Тестируйте оба сценария** (PWA и браузер)
4. **Документируйте** поведение для команды

### ❌ Don't

1. **Не полагайтесь только на User-Agent** (ненадежно)
2. **Не делайте бесконечные редиректы** (проверяйте условия)
3. **Не храните режим в localStorage** (не доступен server-side)
4. **Не показывайте landing в PWA** (плохой UX)

## Метрики для отслеживания

### Analytics события

```javascript
// При установке PWA
gtag('event', 'pwa_install');

// При открытии в standalone
gtag('event', 'pwa_open', { mode: 'standalone' });

// При открытии в браузере
gtag('event', 'pwa_open', { mode: 'browser' });
```

### Полезные метрики

- **PWA Install Rate**: Установок / Визиты
- **PWA Retention**: Повторные открытия PWA
- **PWA vs Browser**: Сравнение конверсии
- **Time to Dashboard**: Скорость доступа

## Roadmap

Планируемые улучшения:

- [ ] A/B тестирование start_url
- [ ] Персонализированный dashboard для PWA
- [ ] Shortcuts API (быстрые действия)
- [ ] Badge API (счетчики уведомлений)
- [ ] Deep linking в PWA
