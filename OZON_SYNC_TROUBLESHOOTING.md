# Устранение проблем синхронизации OZON

Это руководство поможет решить проблемы с синхронизацией товаров OZON.

---

## 📋 Общая информация

### Как работает синхронизация OZON

OZON поддерживает два типа синхронизации:

1. **Получение товаров С OZON** (Pull/Import)
   - Endpoint: `POST /api/ozon/accounts/{account}/sync-catalog`
   - Получает товары из личного кабинета OZON
   - Сохраняет в таблицу `ozon_products`
   - Используется для импорта существующих товаров

2. **Отправка товаров НА OZON** (Push/Export)
   - Endpoint: `POST /api/ozon/accounts/{account}/sync-products`
   - Отправляет товары из SellerMind AI на OZON
   - Требует предварительной настройки категорий и атрибутов
   - Работает батчами по 100 товаров

---

## ❌ Проблема: "No products to sync"

### Причина 1: На OZON аккаунте нет товаров

**Симптомы:**
- При вызове `/sync-catalog` возвращается 0 товаров
- Сообщение: "На OZON аккаунте не найдено товаров для синхронизации"

**Решение:**
1. Проверьте личный кабинет OZON Seller
2. Убедитесь, что товары добавлены и опубликованы
3. Перейдите в раздел "Товары и цены"
4. Проверьте статус товаров (должны быть активными)

---

### Причина 2: API-ключ не имеет прав на чтение товаров

**Симптомы:**
- API возвращает пустой список товаров
- Или ошибку 403 Forbidden

**Решение:**
1. Зайдите в **[ЛК OZON](https://seller.ozon.ru/app/settings/api-keys)**
2. Перейдите в **"Настройки" → "API ключи"**
3. Удалите текущий API-ключ
4. Создайте новый API-ключ с правами:
   - ✅ **Товары** - просмотр и управление
   - ✅ **Цены и остатки** - просмотр и управление
   - ✅ **Заказы** - просмотр
   - ✅ **Финансы** - просмотр
   - ✅ **Аналитика** - просмотр
5. Скопируйте новый Client-Id и API-ключ
6. Обновите credentials в SellerMind AI:
   - Перейдите в **"Маркетплейсы" → OZON → "Настройки"**
   - Вставьте новые Client-Id и API-ключ
   - Нажмите **"Проверить подключение"**

---

### Причина 3: Ошибка при запросе к OZON API

**Симптомы:**
- Ошибка 401 Unauthorized
- Ошибка 404 Not Found
- Ошибка 429 Too Many Requests

**Диагностика:**

Проверьте логи Laravel:

```bash
tail -f storage/logs/laravel.log | grep -i ozon
```

**Решения по типу ошибки:**

#### 401 Unauthorized
```
Client ID или API-ключ недействительны
```
**Действия:**
1. Проверьте правильность Client-Id (должен быть числом)
2. Проверьте правильность API-ключа (должен быть UUID-подобной строкой)
3. Убедитесь, что не скопировали лишние пробелы
4. Создайте новый API-ключ в ЛК OZON

#### 403 Forbidden
```
API-ключ не имеет необходимых прав
```
**Действия:**
1. Пересоздайте API-ключ с полными правами
2. При создании выберите ВСЕ доступные разделы

#### 404 Not Found
```
404 page not found
```
**Возможные причины:**
1. Неверный Client ID
2. **OZON обновил API** - endpoint v2 больше не работает

**Действия:**
1. **Сначала проверьте версию API** - убедитесь что используется `/v3/product/list`, а не `/v2/product/list`
   - См. [OZON_API_V3_UPDATE.md](OZON_API_V3_UPDATE.md) для деталей
   - Версия v3 уже должна быть установлена в коде
2. Если проблема сохраняется - проверьте Client ID в личном кабинете OZON
3. Client ID - это ЧИСЛО (например: 1988192)
4. Обновите Client ID в настройках

#### 429 Too Many Requests
```
Превышен лимит запросов
```
**Действия:**
1. Подождите 1-2 минуты
2. Повторите попытку
3. Не запускайте синхронизацию слишком часто

---

## 🔍 Отладка синхронизации

### Шаг 1: Проверьте подключение к API

```bash
# В терминале SellerMind AI
php artisan tinker
```

```php
// Получите аккаунт OZON
$account = \App\Models\MarketplaceAccount::where('marketplace', 'ozon')->first();

// Проверьте credentials
$creds = $account->getDecryptedCredentials();
dump($creds); // Должно показать client_id и api_key

// Попробуйте ping
$client = app(\App\Services\Marketplaces\OzonClient::class);
$result = $client->ping($account);
dump($result); // Должно вернуть success: true
```

---

### Шаг 2: Попробуйте получить товары вручную

```php
$client = app(\App\Services\Marketplaces\OzonClient::class);
$products = $client->getProducts($account, [], 10); // Получить первые 10
dump($products);
```

**Ожидаемый результат:**
```php
[
    'items' => [
        [
            'product_id' => 123456789,
            'offer_id' => 'SKU-001',
            'name' => 'Название товара',
            // ...
        ]
    ],
    'total' => 100,
    'last_id' => '...'
]
```

**Если items пустой:**
- На OZON аккаунте нет товаров
- Или нет прав на чтение товаров

---

### Шаг 3: Проверьте логи синхронизации

```bash
# Запустите синхронизацию
curl -X POST http://localhost/api/ozon/accounts/1/sync-catalog \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Accept: application/json"

# Смотрите логи в реальном времени
tail -f storage/logs/laravel.log | grep -i "ozon"
```

**Что искать в логах:**

✅ **Успешная синхронизация:**
```
[info] Starting Ozon catalog sync
[debug] Fetching Ozon products page
[debug] Received products from Ozon API: items_count: 50
[debug] Processing Ozon product: product_id: 123456
[info] Ozon catalog synced successfully: synced: 50
```

❌ **Нет товаров:**
```
[info] Starting Ozon catalog sync
[debug] Fetching Ozon products page
[debug] Received products from Ozon API: items_count: 0
[info] No more products to fetch from Ozon: synced_so_far: 0
```

❌ **Ошибка API:**
```
[error] Ozon catalog sync failed: Ozon API error (401): Неверный API Key
```

---

## 📊 Проверка данных в БД

### Проверить количество товаров OZON

```bash
php artisan tinker --execute="echo \App\Models\OzonProduct::count() . ' OZON products';"
```

### Посмотреть последние синхронизированные товары

```php
php artisan tinker
```

```php
\App\Models\OzonProduct::latest('last_synced_at')->limit(5)->get(['id', 'name', 'external_product_id', 'status', 'last_synced_at']);
```

### Удалить все товары OZON (для повторной синхронизации)

```php
// ВНИМАНИЕ: Это удалит все товары из БД!
\App\Models\OzonProduct::truncate();
```

---

## ✅ Пошаговая инструкция по первой синхронизации

### 1. Подготовка в OZON

1. Добавьте минимум 1 товар в личный кабинет OZON
2. Убедитесь что товар опубликован (статус: "Активен")
3. Создайте API-ключ с полными правами
4. Скопируйте Client-Id и API-ключ

### 2. Настройка в SellerMind AI

1. Перейдите в **"Маркетплейсы"**
2. Нажмите **"Подключить OZON"**
3. Заполните:
   - **Название аккаунта**: "Мой OZON"
   - **Client ID**: (число из ЛК)
   - **API ключ**: (UUID из ЛК)
4. Нажмите **"Проверить подключение"**
5. Должно появиться: "Подключение к Ozon API успешно проверено"
6. Нажмите **"Подключить"**

### 3. Синхронизация товаров

1. Перейдите в **"Маркетплейсы" → OZON → "Товары"**
2. Нажмите **"Синхронизировать товары"**
3. Дождитесь завершения (может занять несколько минут)
4. Проверьте количество синхронизированных товаров

### 4. Проверка результата

После успешной синхронизации вы увидите:
- Список товаров с OZON
- Название, артикул (offer_id), цена, остатки
- Статус товара (processing/published/archived)

---

## 🔧 Частые проблемы

### Проблема: Товары синхронизируются, но показывают статус "processing"

**Причина:** Товары на OZON находятся на модерации

**Решение:** Дождитесь окончания модерации в ЛК OZON

---

### Проблема: Синхронизация занимает очень много времени

**Причина:** Много товаров на OZON (>1000)

**Решение:**
- Синхронизация работает правильно
- Для 1000 товаров требуется ~5-10 минут
- Не прерывайте процесс
- Следите за логами

---

### Проблема: Не все товары синхронизировались

**Возможные причины:**
1. Товары были добавлены после синхронизации → повторите sync
2. Товары в статусе "Черновик" → опубликуйте их в ЛК
3. Товары заблокированы модерацией → проверьте в ЛК
4. Ошибка API для конкретных товаров → проверьте логи

**Решение:**
1. Запустите синхронизацию повторно
2. Проверьте логи: `tail -f storage/logs/laravel.log | grep "WARNING"`
3. Проверьте статус товаров в ЛК OZON

---

## 📞 Дальнейшие действия

### Если проблема не решена:

1. **Соберите диагностическую информацию:**
   ```bash
   # Проверьте подключение
   curl -X POST http://localhost/api/ozon/accounts/1/test \
     -H "Authorization: Bearer TOKEN"

   # Попробуйте синхронизацию
   curl -X POST http://localhost/api/ozon/accounts/1/sync-catalog \
     -H "Authorization: Bearer TOKEN"

   # Проверьте логи
   tail -100 storage/logs/laravel.log | grep -i ozon > ozon_debug.log
   ```

2. **Проверьте документацию OZON:**
   - [OZON Seller API](https://docs.ozon.ru/api/seller/)
   - [Получение токена](https://seller.ozon.ru/app/settings/api-keys)

3. **Обратитесь в поддержку:**
   - Email: support@sellermind.ai
   - Приложите файл `ozon_debug.log`
   - Укажите Client-Id (но НЕ API-ключ!)

---

**Дата последнего обновления**: Декабрь 2024
